
git-checkout . || { echo error; exit -1; }

while :; do
  echo "next patch to process: "`quilt next`
  read -p "apply it?"
  git-am -i patches/`quilt next`
  read -p "remove from series file?"
  rm -rf .pc
  quilt del -n 
  echo "done!"
  echo
done
