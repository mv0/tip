#!/bin/sh
#
# Push the current branch to the kernel.org repository
#

. $(dirname "$0")/tip-lib
B=`get_current_branch`

echo $B

git push $@ origin $B && {
  ssh linux.kernel.org 'cd tip; git update-server-info;'
}

exit $?
